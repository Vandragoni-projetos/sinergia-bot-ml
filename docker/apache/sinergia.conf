<VirtualHost *:8080>
    DocumentRoot /var/www/app/public
    ServerSignature Off

    <Directory /var/www/app/public>
        AllowOverride All
        Require all granted
        Options -Indexes
    </Directory>

    # Somente public/ é servido; código, .env e storage ficam fora do DocumentRoot.
    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined
</VirtualHost>
