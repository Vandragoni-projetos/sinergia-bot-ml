{% extends 'panel/layout.twig' %}

{% block content %}
<ol class="flow" aria-label="Como o bot funciona">
    <li>Conectar</li>
    <li>O que vender</li>
    <li>Onde publicar</li>
    <li>O bot trabalha</li>
</ol>

<h1>{{ page.title }}</h1>
<p class="muted">{{ page.lead }}</p>

<section class="placeholder">
    <p>Esta área chega {{ page.arrives }} do plano. Por enquanto, nada é configurado aqui.</p>
</section>
{% endblock %}
